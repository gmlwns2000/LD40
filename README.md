# Source for my Ludum Dare 40 entry

## Project layout
- `external/`: dependencies
- `src/`: headers and source files
- `bin/`: binaries
- `build/`: solution, project files

## CMake ?
I'm lazy to set it up. Yes, I'm that lazy.